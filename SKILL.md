---
name: tvskill
description: 把中文竖屏短剧剧本走完整条 AI 生产链——提取角色场景道具、生产人物设定图与场景色卡等资产、编译分镜提示词、直到成片验收返工——最终交付一份可人工复制到 LibTV 视频生成节点的 Markdown 成品。媒介可选真人实拍、2D 动漫、3D CG 或其它，全剧统一声明后不中途更换。内置 v3 导演、对白零改写、空间连续和 LLM 自主定时方法；使用独立人物参考图、干净首帧/验收续接帧、LibTV 的 @语义资产与 {{Mixed N}} 绑定；为 Seedance 2.0 编译低自由度表演、明确眼神目标、正式声音路线、场景功能拓扑、家具与人体朝向、群像坐姿、全剧提示词二审、跨集连续性门禁和成片返工标准。用户提到 tvskill、剧本转短剧、角色场景提取、人设图或资产生成、v3 分镜转 LibTV、三合一提示词、LibTV 完成提示词、全剧连贯性检查、空间或朝向穿帮、黑板座位方向、群像反坐、Seedance 2.0 提示词优化、表演去 AI 感、人工复制视频节点提示词或短剧视频提示词交付时使用。
---

# TVSkill

使用本目录内置的 v3 导演知识，把下游改为 LibTV 人工视频生成节点。唯一可见产物是 Markdown，不交付 JSON。

## 独立迁移合同

- `tvskill/` 必须自包含；复制整个目录到另一台电脑后，不需要安装、读取或调用任何其他 skill。
- 所有运行时知识只从本目录 `references/`、`assets/` 和 `scripts/` 读取，路径必须相对 `SKILL.md` 解析。
- 本 skill 可以使用目标电脑已有的 Python 3 标准库；只有用户要求操作画布时才需要目标电脑安装并登录 LibTV CLI。
- 文档中的资料来源只用于溯源，不能写成运行时外部依赖、绝对路径或跨 skill 路由。

## 固定合同

- 每集只输出一个 `<集号>-LibTV视频节点提示词.md`。
- 不生成或交付 `candidate.json`、`asset-plan.json`、`movement-ledger.json`、`reference-manifest.json`。
- 所有视频生成节点默认使用模型 `Seedance 2.0 Fast VIP`、画幅 `9:16`、分辨率 `480P`；只有用户明确指定其它参数时才覆盖。
- Markdown 中每一段对应一次 LibTV 视频生成，段内 `text` 代码块是可整块复制的最终提示词。
- 最终提示词必须使用 `@[语义资产]` 和 `{{Mixed N}}`；`Mixed N` 按该段实际上传/连接的素材顺序从 1 连续编号。
- 每段必须标记交付等级、制作路线、风险标签、运行状态和连续性模式。正式片段只有通过生成前五道闸门后才能标为“可运行”。
- Mixed 只绑定可直接喂给模型的干净生成资产。位置图、轨迹图、构图图、平面图、箭头图、文字标注图和未验收尾帧只用于内部规划，严禁进入 Mixed。
- 人物、音色、干净首帧/已验收续接帧、场景状态和关键道具状态在段头绑定。每个可识别人物必须使用独立人物图；不同人物和不同职责资产不得共用 Mixed。未提供的素材保留明确上传槽位，不编造 URL、节点 Key 或资产 ID。
- 精确台词、OS、VO、旁白、系统音和人物关系不得新增、遗漏、换序、重复或改写。
- 任何包含台词、OS、VO 或旁白的节点都必须开启声音，并为每一名实际说话人绑定已审核的独立音色音频；缺少任一说话人音色时标为阻塞，不得同步为可生成状态。禁止静音画面、无音频模型盲合成、多人共用同一音色或用另一角色音频代替。
- 同一连续组按顺序生成。只有前段验收通过并登记实际出点后，后段才能绑定该段的干净续接帧；未验收片段不得作为下游参考。
- 默认只生成 Markdown，不创建、不更新、不运行 LibTV 节点。只有用户明确要求操作画布时才执行 CLI；运行视频仍需具体节点授权。
- 不调用 `libtv script storyboard`，不把 `script` 节点作为交付中心。
- v3 文件只提供导演读解、切分、空间和表演方法；当其中的旧模型表面语法与 LibTV 合同冲突时，以本文件及 `references/libtv/` 的最终交付语法为准。

## 开始前

最低输入是一集剧本正文。处理全剧或用户要求全剧连贯性时，读取项目中全部可用剧本、
既有提示词和已验收状态；资料不足不阻止生成当前集，但必须缩小并如实标注审核范围。
本地已有资料先自行发现，不重复向用户索取。

不要索取统一 `DURATION_SEC`。LLM 先按台词、动作、反应和剪辑落点判断每段时长，通常为 4–15 秒；若用户指定的模型有更窄时长集合，再以该模型实时 schema 为准。

## 必读顺序

1. 完整读取 [v3 → LibTV 映射](references/v3-to-libtv-mapping.md)。
2. 完整读取 v3 的 [输入归一化](references/v3/01-input-normalization.md)、[实体提取](references/v3/02-entity-extraction.md)、[时长与切分](references/v3/03-duration-and-storyboard-splitting.md)、[景别运镜](references/v3/04a-shot-scale-and-camera-library.md)、[调度构图光线](references/v3/04b-blocking-composition-lighting.md)、[约束编译](references/v3/04c-constraint-and-negative-prompt-compiler.md)、[镜头编写](references/v3/04-shot-writing-and-directing.md)、[台词与声音](references/v3/05-dialogue-and-audio.md)和[标签风格](references/v3/06-tags-and-style-control.md)。
3. 完整读取 [制作就绪与返工合同](references/libtv/production-readiness-and-retake-contract.md)、[全剧提示词二审合同](references/libtv/series-continuity-audit-contract.md)、[LibTV 完成提示词规范](references/libtv/libtv-completed-prompt-format.md)、[Seedance 2.0 官方指南知识快照](references/libtv/seedance-2.0-official-guide-snapshot.md)、[Seedance 2.0 官方提示词优化适配](references/libtv/seedance-2.0-official-prompt-optimizer.md)、[真人表演、眼神与对白合同](references/libtv/performance-dialogue-contract.md)、[场景拓扑与功能朝向](references/libtv/scene-topology-and-functional-orientation.md)、[光影构图与画面风格母版](references/libtv/visual-look-and-style-bible.md)、[生成成片质量审计](references/libtv/generated-take-quality-audit.md)、[人工复制交付合同](references/libtv/manual-video-node-delivery.md)和 [Seedance 适配](references/libtv/seedance-2.0-adaptation.md)。
4. 完整读取 [万物生实战 Craft 补充](references/libtv/wanwu-field-craft.md)：过冲量、反英雄景别与音画距离耦合三项官方规范未覆盖、但被真实上线成片反复验证的手法；与既有合同冲突时以既有合同为准。
5. 项目没有现成素材、需要先造资产时，完整读取 [资产生产合同](references/libtv/asset-production-contract.md)。
5. 只有用户要求实际操作 LibTV CLI 时，才读取 [LibTV 画布合同](references/libtv/libtv-canvas-contract.md)并核对本机版本与模型 schema。

## 核心知识库模块

- 剧本与时长：内置 v3 归一化、原子事件和生成 Shot 预算；
- 表演与声音：眼神、身体层级、对白、音色和声画同出；
- 空间与连续性：场景母版、功能拓扑、家具/人体朝向、轴线和真实出点；
- 摄影与质感：`STYLE-ID`、`LOOK-ID`、光线、构图、材质和运动节奏；
- LibTV 执行：Mixed 绑定、画布实时回读、运行前指纹凭证；
- 生成后验收：全部版本回收、十轴审计、全剧装配和单变量返工。

`scene-topology-and-functional-orientation.md` 是空间模块的强制组成部分，不能因已有场景图、
位置图或普通轴线账本而省略。缺失该文件时，tvskill 不具备完整迁移资格。

## 工作流

### 1. 建立内部账本

按原文顺序建立语音账本，覆盖所有有文本声音。建立导演弧线、场景母版、人群占位状态、原子镜头起落幅、人物动线、眼神对象、光线、道具实例与状态、相邻切点。对白节拍额外记录说话人的目标、障碍、策略、潜台词、开口触发、具体眼神目标、零或一个有动机的身体变化、零或一个必要声音控制和说完后的活状态。账本只用于推理，不作为独立文件交付。

全剧先锁一个 `STYLE-ID`，每个逻辑场锁一个 `LOOK-ID`，记录主光物理来源与世界方向、
对比和色彩、肤色与材质、镜头倾向、构图权力、运动节奏及同场变体。连续组重复同一条
短风格脊柱，不在各段随机更换“电影感”同义词。

同一逻辑场跨多段时规划场景包；锁定世界坐标、轴线、人物位置、主光方向、人群占位和关键道具实例。功能场景还必须锁定功能前方、家具朝向和人体默认使用方向：例如教室黑板/讲台在前墙，课桌、座椅和就座学生的骨盆、膝盖、双脚朝向黑板。相似道具必须编号，例如“钢笔-A（扎墙）”“钢笔-B（手中）”“报告-A-S0/S1/S2”，不得用一个模糊资产同时承担互斥状态。

### 1b. 提取实体与生产资产（无现成素材时）

用户已提供全部素材时跳过本步；否则按 [实体提取](references/v3/02-entity-extraction.md) 与 [资产生产合同](references/libtv/asset-production-contract.md) 补齐上游：

1. 从账本导出实体清单：可识别人物及其互斥形态、逻辑场及其人数与光线状态、承担叙事功能的道具及其互斥状态、每场色板、全剧媒介；
2. 按“镜头看到几个面就锚几个面”判定每个实体的资产形态：固定单侧用单图，有转身、越肩、环绕或说话转头则用多视图；互斥形态与互斥状态各自独立成资产；
3. 为每个资产写结构化图生成提示词：格式、设定、世界统一 DNA、逐部位 CRITICAL、分格、光线、风格参考、失败模式定向负向；
4. 先低清试拍确认识别特征立得住，再高清定版；定版并经人工确认后写回账本作为 canonical；
5. 资产未验收前不得进入 Mixed 绑定，不得作为 canonical 被下游引用。

规划图（位置图、轨迹图、平面图、箭头图、故事板）只服务导演推理，永远不进 Mixed。

### 2. 切分生成段

每段只承担一个清楚的叙事任务。边界落在完成动作、完整意群、明确反应或自然转场。

- 先按官方任务分类判断多模态参考、编辑、延长或组合；只有 LibTV 实时 schema 暴露
  对应能力时才使用编辑/延长语义；
- 多模态参考再按时间与空间两个轴判断复杂度：两者都少才用单镜；多事件链、状态递进、
  对话往返、换机位或穿越多个区域任一成立时，优先在同一节点内使用多镜路径；
- 素材数量、节点秒数和是否已有镜号都只能辅助判断，不能因为 5–8 秒就强制判为单镜；
- 先按约 4–5 个汉字/秒核算台词；
- 再加入动作完成、反应、稳定起幅和完成落幅；
- 选择足够而不过度拖长的整数时长；
- 默认 4–15 秒，超出时在自然完成点拆段；
- 内部时间账本覆盖完整段长，但绝对时间码不得进入最终模型代码块。
- 同步对白镜头严格使用“每个 Shot 一位说话人一个自然意群”；只有单一事件时才要求单一连续镜头，多镜节点允许在不同 Shot 依次说话；
- 原生同步对白段不同时承担走位、转身、递物、高速道具、群体反应、精确文字、OS/VO 或复杂特效；
- 多镜声画同出节点中，每个 `Shot N:` 只允许一位说话人和一个自然意群；OS/VO 必须独立落在没有口型对白的 Shot；
- 每个 `Shot N:` 只选择一个主要保真目标：人物对白、复杂动作、群体状态或关键道具之一；其它层保持稳定；
- 一句超过一个自然换气意群或同时承担两次情绪转折时，在完整意群处拆段；
- 高速道具、精确文字、复杂接触和群演高风险变化仍分别拆段。

v3 的 2–3 秒内部剪辑节拍不等于模型 `Shot N:` 数量。原生 Seedance 多镜按生成事件预算：
10–15 秒通常只承载 2–3 个清楚 Shot，每个 Shot 约 4–6 秒完成一个动作、对白或反应。
内部剪辑点更多时，优先拆节点或交给后期，不把密集剪辑清单全部塞进一次生成。

同一时空的连续事件先尝试合并为一个多镜节点，避免人物在独立生成节点之间瞬移。单场
长对话、情绪递进或单一路径移动，若 LibTV 暴露视频延长能力，优先用上一段已验收视频
执行“向后延长”。只有剧情转折、新场景或高风险任务才采用受控独立节点。

### 3. 分配 Mixed 素材

先把资产分为“规划用”和“生成用”。规划图只帮助导演确定空间，绝不进入 Mixed。逐段按实际需要排列生成资产：

1. 主要人物独立身份图、按需使用的独立三分之四侧脸近照，以及该段开口人物各自独立音色；
2. 其他可识别人物各自独立的身份图，以及其中开口人物各自独立音色；
3. 干净首帧或上一段已验收的干净续接帧；
4. 与当前人数一致的场景状态图；
5. 当前状态的关键道具图或单一动作参考。

同一资产可在段内复用，但不同人物或不同资产类型不能共享 Mixed。全班可见时必须使用
经过视觉验收的“占座＋功能朝向”场景状态图，能看清黑板/讲台、桌椅统一朝向、学生坐姿、
门窗和主过道；空教室图不得与可见群演要求同段，也不能单独承担群像坐姿方向。
所有 `{{Mixed N}}` 从 1 连续递增；语义名保持稳定。

对白人物只有正面立正、直视镜头的全身图时，不能直接进入正式近景对白。保留该图承担身份与服装，同时增加离镜视线的三分之四侧脸图或干净过肩首帧；否则降级为预览。任一文本声音没有合法音色素材时，节点保持阻塞，不得以关闭声音绕过。

可从同一项目已验收的历史 Seedance 2.0 成片建立音色资产：为每名角色选取只有该角色
说话、无其他人声、无音乐且背景声尽量低的连续有效人声，裁成约 5 秒，导出 48 kHz 单声道
WAV，并以“角色名-历史成片音色参考-5s”独立上传。不得把多人对话整段、含串音片段或
同一音频绑定给多个角色。提示词必须声明该音频只控制指定 `<主体N>` 的音色，不继承
原台词、背景声、情绪和节奏；原台词仍以当前 `{}` 内容为唯一真值。

选好单人说话时间窗后，用内置脚本确定性裁切并校验：

```bash
python3 scripts/extract_voice_reference.py <历史成片.mp4> <角色名-历史成片音色参考-5s.wav> \
  --start <有效人声起点秒数> --duration 5
```

脚本依赖本机 `ffmpeg`，只负责格式、时长和响度处理，不能自动判断是否串入其他人声；
上传前仍须人工试听确认单一说话人、无音乐和无明显背景干扰。

### 4. 编写 LibTV 完成提示词

最终提示词使用紧凑三段论：

1. **总体设定与资产定义**：按官方主体定义法绑定独立人物图；TVSkill 默认统一为 `<主体N>`，角色真名保留在 `@[角色名] {{Mixed N}}` 中。把当前人数场景状态定义为 `<场景N>`，用干净首帧/续接帧锁定站位、眼神和道具状态。
2. **镜头动作**：连续单镜写明“单一连续镜头，无剪切”，不使用镜号；原生音频同步对白包含开口触发、具体眼神对象、精确台词和活状态。身体变化与声音控制都允许为零且各最多一个。短台词使用一口自然说完，不写放慢、停半拍、一字一顿或拖长。时间或空间维度为“多”时使用精确 `Shot 1:` / `Shot 2:` 标签，可在冒号后补中文分镜名；每个含文本声音的 Shot 只出现一名说话人并使用其独立音色，每个 Shot 只有一个主要保真目标；10–15 秒通常为 2–3 个 Shot。禁止旧 `镜头N：` 标签，也不能把连续镜头合同与 `Shot N:` 混用。
3. **风格与约束**：沿用本场 `LOOK-ID` 的短风格脊柱，写明物理光源、构图关系、真实材质和运动节奏，再保留连续性、零文字、水印/Logo 和必要的多人身份约束；分辨率交给节点参数，不堆叠空泛画质词或普通缺陷词。

多人镜头采用三级动作：一个焦点人物完成主要动作，最多一人完成一次小反应，其余人物只保持呼吸、眨眼、轻微肩部变化或自然视线漂移。不得写全场同时转头、同时僵住或同时后退。

功能场景的群像反应还要锁住身体层级。教室里“学生看向某人”默认只移动眼睛、头部和
最多少量肩部；骨盆、膝盖、双脚、座椅和课桌仍朝黑板。后排朝前墙机位可见黑板时，
正常就座学生只能呈背面或后侧三分之四；学生正脸完整可见时，黑板应在摄影机后方或改用
侧面机位。不能为了正脸让学生整排反坐。

台词使用 `{}`，音效使用 `<>`，BGM 使用 `（）`；零文字任务禁止在代码块中使用 `【】`。完整遵循 [官方提示词优化适配](references/libtv/seedance-2.0-official-prompt-optimizer.md)。

### 5. 生成 Markdown

复制 [交付模板](assets/libtv-video-prompts.template.md)并严格遵循 [人工复制交付合同](references/libtv/manual-video-node-delivery.md)：

- 全集摘要、段数和总时长；
- 节点默认模型 `Seedance 2.0 Fast VIP`、画幅 `9:16` 与分辨率 `480P`；
- 公共素材清单；
- 按顺序排列的全部生成段；
- 每段 Mixed 上传顺序；
- 每段交付等级、制作路线、风险标签、运行状态、连续性模式、声音开关和文字策略；
- 每段逐说话人音色绑定状态；有文本声音却缺任一音色时写明阻塞；
- 每段一个可整块复制的最终提示词代码块；
- 相邻段衔接检查；
- 原始语音逐句对账。

### 6. 全剧二次导演检查

提示词初稿完成后不得直接交付。读取同项目全部可用剧本、既有集 Markdown 和已验收
观察状态，严格执行 [全剧提示词二审合同](references/libtv/series-continuity-audit-contract.md)
的七遍审核：原文准确性、原子任务、资产职责、段内一致性、段间连续性、跨集连续性和
模型可执行性。

先生成问题清单，再修改提示词、Mixed、制作路线和状态交接；修改后从第一遍重新检查。
全剧资料齐全时统一审核全部集；只提供单集时只能写“截至当前集”或“单集”，不得假定
未来剧情。每集 Markdown 保留全剧连续性声明、连续性母版、每段连续组/前置段和状态
交接表，但不另交付 JSON。

### 7. 确定性校验

```bash
python3 scripts/validate_delivery_md.py <集号>-LibTV视频节点提示词.md
python3 scripts/audit_series_continuity.py <剧集提示词目录或多个Markdown>
python3 scripts/validate_skill_portability.py .
```

修正全部错误，直到退出码为 0。只有确定性校验和 LLM 七遍语义二审都通过，才能把
`全剧二审` 与各段 `提示词二审` 写为 `已通过`。脚本通过不代表模型成片合格。

### 8. 画布两阶段预检

只有用户明确要求发送到 LibTV 画布时执行本节，且创建/更新节点与运行生成必须分成两个阶段：

1. 创建或更新**未运行**的视频节点，连接各段实际生成资产；不得包含 `--run`；
2. 重新读取每个节点的 `prompt`、`mixedList`、`mixedListOrder`、`audioList`、模型和 settings；
3. 不信任提示词文本或上传表中自报的 `Mixed N`，必须按画布实时 `mixedListOrder` 解析第 N 位实际素材，并核对语义标签、媒体类型与唯一职责；
4. 对目标节点执行只读预检：

```bash
python3 scripts/sync_delivery_markdown.py <本集.md> \
  --project <PROJECT_UUID> --node-suffix=-v3提示词
# 核对 dry-run 后，用户已明确要求写画布时再追加 --apply

python3 scripts/audit_canvas_nodes.py --project <PROJECT_UUID> --name-prefix <节点名前缀>
```

也可用多个 `--node <NODE_ID>` 精确限定节点。任何硬错误都必须修复、回读并重审；只有
`errors=0` 才能报告“画布提示词已就绪”。准备运行时必须追加 `--pre-run` 和
`--receipt <审计凭证.md>`；凭证记录当前节点指纹，节点 prompt、Mixed、模型、settings
或任务状态发生变化后立即失效。已有生成结果的节点若未通过审计，其成片和尾帧一律视为
拒绝态，不能进入连续性链。

只有用户另行明确授权运行具体节点后，才按同一连续组顺序生成。不得并行修改同一画布；
写入后必须以回读事实为准，不能用计划表或 CLI 成功返回替代验收。

### 9. 顺序生成、成片回收、验收与返工

同一连续组只运行当前“可运行”的第一段。生成后逐段检查身份、眼神目标、口型与逐字台词、动作边界、人群数量、道具实例/状态、空间轴线、画面文字和相邻可剪辑性。任何硬失败必须标为拒绝，不能作为下一段参考。

每轮生成完成后，按 [生成成片质量审计](references/libtv/generated-take-quality-audit.md)
下载目标前缀下所有已生成版本，不只看最新节点。保留节点原名，生成接触表并读取音轨；
逐片执行节点参考、人物、表演眼神、对白声音、空间人群、道具物理、镜头剪辑、光影质感、
画面卫生和边界连续性十轴审计。离线 ASR 只定位复听疑点，不代替人工逐字验收。

单片通过后还要按剧本顺序装配，检查同场人口、空间、光线、色彩、音色、动作因果和
相邻首尾帧。只有“保留”片段可输出稳定续接帧；预览、后期候选、重抽和重写片段都不能
自动进入连续性链。

返工必须改动失败变量：更换干净首帧、拆段、调整制作路线、补充或更换角色音色、改用后期文字或替换冲突资产。相同硬失败再次出现时，不允许只追加“不要……”类否定词重跑。验收通过后才导出干净稳定续接帧，并与 canonical 人物、场景和道具母版共同使用；尾帧只允许一跳，每两到三段重新锚定 canonical。

## 交付标准

- 交付目录只新增一个 Markdown 成品，不新增 JSON；
- Markdown 包含真实审核范围、全剧连续性母版、每段前置依赖和状态交接；
- 全剧资料齐全时跨全部集二审；资料不全时明确写“截至EPXX”或“单集”；
- 确定性校验与七遍语义二审全部通过后才允许标记“已通过”；
- 生成段编号连续，总时长等于各段时长之和；
- Markdown 明确标注默认模型 `Seedance 2.0 Fast VIP`、画幅 `9:16`、分辨率 `480P`；
- 每段 Mixed 上传表与代码块中的 `{{Mixed N}}` 一一对应且连续；
- 发送画布后，`Mixed N` 还必须与实时 `mixedListOrder` 第 N 位的实际素材语义一致，画布只读审计为零硬错误；
- 运行前审计凭证必须与当前节点指纹一致；任一字段变化后重新审计，不沿用旧凭证；
- 段时长只存在于 Markdown 与节点参数，代码块不出现绝对时间码；
- 声音默认开启；每个含文本声音的 `Shot N:` 只有一位说话人和一个自然意群，且绑定该人物独立音色；
- 同步对白包含开口触发、具体眼神对象、精确台词和活状态；零或一个身体变化、零或一个声音控制；
- 多人镜头只设一个表演焦点，背景人物使用持续微动，不出现同步群体反应；
- 功能场景的前方、家具朝向与人体默认朝向一致；教室群像绑定已验收的占座教学朝向图，
  学生回看只分级转动眼、头、肩，不改变骨盆、膝盖和课桌朝向；
- 每个可识别人物使用独立人物图，规划图不进入 Mixed，干净生成资产各司其职；
- 台词、OS、VO、旁白逐说话人绑定合法独立音色；缺任一音色即阻塞，不得关闭原声绕过；
- 台词逐字保留且只出现一次；
- 相邻段的人物、轴线、道具、光线、环境声和动作状态可剪辑衔接；
- 同一场景连续组沿用同一 `LOOK-ID`，主光方向、色彩、材质和运动节奏无随机漂移；
- 全班/人群可见时使用已占位场景状态；精确文字只用定版道具图或后期叠加；
- 硬失败片段不交付、不续接；续接帧来自已验收片段且只允许一跳；
- 每轮生成后回收所有已生成版本并完成十轴逐片审计与按剧本顺序的装配复核；
- 所有段显式声明全剧统一的媒介（真人实拍／2D 动漫／3D CG／其它），媒介与资产图一致且中途不更换；物理光源、零字幕、零水印、零 Logo，不用空泛画质套话替代表演；
- 未获明确授权时，不写画布、不运行生成。
