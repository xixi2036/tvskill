---
name: tvskill
description: 把中文竖屏短剧剧本走完整条 AI 生产链——提取角色场景道具、生产人物设定图与场景色卡等资产、编译分镜提示词、直到成片验收返工——最终交付一份可同步到 TVMao 视频生成节点的 Markdown 成品。媒介可选真人实拍、2D 动漫、3D CG 或其它，全剧统一声明后不中途更换。内置 v3 导演、对白零改写、空间连续和 LLM 自主定时方法；使用独立人物参考图、干净首帧/验收续接帧，把兼容 Markdown 的 {{Mixed N}} 确定性编译为 TVMao node-id mention，并按实时有序入边序列化为 Seedance 图片N/视频N/音频N；为 Seedance 2.0 编译低自由度表演、明确眼神目标、正式声音路线、场景功能拓扑、家具与人体朝向、群像坐姿、全剧提示词二审、跨集连续性门禁和成片返工标准。用户提到 tvskill、剧本转短剧、角色场景提取、人设图或资产生成、v3 分镜转 TVMao、TVMao CLI、全剧连贯性检查、空间或朝向穿帮、Seedance 2.0 提示词优化、人工复制视频节点提示词或短剧视频提示词交付时使用。
---

# TVSkill

使用本目录内置的 v3 导演知识，把下游接到 TVMao 视频生成节点。唯一可见交付物是 Markdown，不交付 JSON。

## 独立迁移合同

- `tvskill/` 必须自包含；复制整个目录到另一台电脑后，不需要安装、读取或调用任何其他 skill。
- 所有运行时知识只从本目录 `references/`、`assets/` 和 `scripts/` 读取，路径必须相对 `SKILL.md` 解析。
- 本 skill 可以使用目标电脑已有的 Python 3 标准库；只有用户要求操作画布时才需要目标电脑安装并登录 TVMao CLI。
- 文档中的资料来源只用于溯源，不能写成运行时外部依赖、绝对路径或跨 skill 路由。

## 固定合同

- 每集仍输出一个 `<集号>-LibTV视频节点提示词.md`，文件名暂为兼容既有状态机和 110 项校验保留；画布后端已经是 TVMao。
- 不生成或交付 `candidate.json`、`asset-plan.json`、`movement-ledger.json`、`reference-manifest.json`。
- Markdown 为兼容历史交付仍写模型 `Seedance 2.0 Fast VIP`、画幅 `9:16`、分辨率 `480P`；TVMao 同步器把该展示名映射到稳定 `modelId=doubao-seedance-2-0-fast-260128`，并在每次写入前实时校验 schema。用户指定其它模型或参数时必须先通过 schema，不得仅改 Markdown 绕过。
- Markdown 中每一段对应一次 TVMao `video-generator` 生成，段内 `text` 代码块是编译前的最终提示词。
- Markdown 最终提示词继续使用 `@[语义资产] {{Mixed N}}` 作为兼容内部 DSL；`Mixed N` 按表格总顺序从 1 连续编号。写入 TVMao 时必须编译为画布 canonical `@[图片:<nodeId>]/@[视频:<nodeId>]/@[音频:<nodeId>]`，语义标签紧随 token；网页运行时再按实时入边顺序序列化为 `图片N/视频N/音频N`，三类媒体各自编号。
- 每段必须标记交付等级、制作路线、风险标签、运行状态和连续性模式。正式片段只有通过生成前五道闸门后才能标为“可运行”。
- Mixed 只绑定可直接喂给模型的干净生成资产。位置图、轨迹图、构图图、平面图、箭头图、文字标注图和未验收尾帧只用于内部规划，严禁进入 Mixed。
- 人物、音色、干净首帧/已验收续接帧、场景状态和关键道具状态在段头绑定。每个可识别人物必须使用独立人物图；不同人物和不同职责资产不得共用 Mixed。未提供的素材保留明确上传槽位，不编造 URL、节点 Key 或资产 ID。
- 精确台词、OS、VO、旁白、系统音和人物关系不得新增、遗漏、换序、重复或改写。
- 任何包含台词、OS、VO 或旁白的节点都必须开启声音，并为每一名实际说话人绑定已审核的独立音色音频。音色未定不阻塞上游：先按段头写明音色占位槽，剧本、资产、提示词和 Markdown 照常推进；占位槽未由人工上传音色并完成关联前，该段运行状态保持阻塞，不同步、不运行。禁止静音画面、无音频模型盲合成、多人共用同一音色或用另一角色音频代替，也禁止助手替用户挑定最终音色。
- 同一连续组按顺序生成。只有前段验收通过并登记实际出点后，后段才能绑定该段的干净续接帧；未验收片段不得作为下游参考。
- TVMao `image-generator` 与视频节点使用不同的引用合同：图像节点的真实参考来自有序入边，当前产品不在 prompt 编辑器中渲染 node-id mention。图像提示词只能写 `参考图N（语义名）`，N 必须与 `edge list --to` 顺序一致；禁止普通 `图片N/图N`、禁止把 `@[图片:nodeId]` 硬塞进图像 prompt、禁止把 `A01/EP01/v02` 等内部编号写进模型正文。运行有父图的图像节点前必须执行 `scripts/audit_image_asset_node.py --pre-run`，确认父图 `succeeded` 且有实际输出。
- 资产生成必须按画布依赖 DAG 分层授权。一次“确认生成”只授权当前最浅的可运行层，不得自动级联到下一层。当前层全部结束并完成视觉验收后必须暂停：支持结构化用户输入工具时弹窗提供“通过本层并生成下一层（推荐）／返工本层／停止”三项；工具不可用时用同样三项向用户明确询问并等待。未收到新确认时，任何子节点都不得运行。
- “3D CG”不是完整画风选择。创建正式资产 DAG 前必须先让用户选择3D子风格（例如影视级国漫风格化3D／高端日韩风格化3D／仿真人数字人），再只生成一张低成本风格定调图。用户验收通过后将该图登记为 STYLE-ID 的真实父图，并连接到首层正式资产；未通过时只返工定调图，不批量生成资产。禁止用“电影级、高精、PBR”这类共性词替代子风格选择。
- 人物“标准参考图”和“人物剧照/表演锚”必须分开。带场景、扶门框/端坐等剧情动作、看向画外对象、戏剧光位或明确情绪表演的单幅人物图只能登记为人物剧照/表演锚，禁止冒充身份根资产。3D 动画的标准人物参考图默认采用纯净背景的官方角色设定展示板：正面头肩肖像 + 全身正面/侧面/背面三视图，人物中性站姿、无剧情动作、无场景道具、无文字水印；它位于剧照上游。标准图验收后才可派生单人全身身份图、独立脸部图和人物剧照，标准展示板本身不得直接进入视频 Mixed。
- 色卡参考图固定使用万物生标准版式：`16:9` 横版白底、色块单行从左到右排列、每格下方以黑色等宽字体标注 `#RRGGBB`；色块纯色硬边，无渐变、纹理、噪点或阴影。色块数量默认 **13 色**（正传标准档）；逻辑场角色/生物/光位数量多、13 色不足以锚定全部主光/补光/硬阴影/各角色配色/环境时，可升级为 **22 或 24 色**（问心复杂场景实证档，如废墟战场、巨兽多角色同框）——版式约束不变，只放开色块数量，仍必须是 13/22/24 三档之一，不得任意取数。色卡属于参考资产，其版式与最终视频的 `9:16/16:9` 画幅无关；项目只替换所选档位的颜色及各自用途，不得改成竖版、上下堆叠、无 HEX 标签，或色块数量既不是 13 也不是 22/24。生成后必须打开实际输出逐格数满所选档位色块数，并按从左到右顺序逐字核对每个 HEX；节点 `succeeded` 或 prompt 审计通过都不能代替视觉验收。
- 默认只生成 Markdown，不创建、不更新、不运行 TVMao 节点。只有用户明确要求操作画布时才执行 CLI。TVMao CLI 2.0.0 的 `node run` 尚未序列化画布 node-id mention，TVSkill 节点即使获得具体授权也必须从网页运行；CLI 修复并通过源码同构测试前禁止绕过。
- 不创建脚本中间节点，不把画布节点当作交付中心。
- v3 文件只提供导演读解、切分、空间和表演方法；当旧 LibTV 表面语法与 TVMao 合同冲突时，以本文件及画布合同的编译规则为准。

## 入口：流程由状态机决定，不由你判断

**每次开工的第一条命令，不是读剧本，是问状态机现在该做哪一步：**

```bash
python3 scripts/pipeline_state.py status <集号> --dir <交付目录>
```

它会打印九个步骤的完成情况、下一步是什么、本步的闸是什么。**照它说的做，不要自行决定
从哪里开始、也不要因为"这步看起来已经做过了"而跳过。** 每一步必须过闸才能标记完成：

```bash
python3 scripts/pipeline_state.py check    <集号> <步骤> --script <原剧本> --episode-no <集号数字>
python3 scripts/pipeline_state.py complete <集号> <步骤> --script <原剧本> --episode-no <集号数字>
```

`check` 只跑闸，`complete` 过闸才写状态。前置步骤没完成时下游步骤直接拒绝执行——
**跳步在这里是物理上走不通的，不是"不建议"。**

| 步骤 | 做什么 | 闸检查什么 |
|---|---|---|
| `intake` | 前置决策问答（媒介/范围/资产现状/画布操作/音色状态，见「开始前」一节） | 产出 `<集号>-任务前置决策.json`，必填项非空 |
| `script_units` | 抽取原剧本的画面单元 | 必须传 `--script` 真读剧本，产出 `<集号>-画面单元.json` |
| `entities` | 提取人物/场景/道具/色卡 | 四类**都要有显式声明**，本集没有的类别也要写一行"无＋已核对" |
| `assets` | 资产生产与验收 | 每项资产写明形态；含精确文字的道具必须是定版道具图 |
| `segments` | 切分生成段、编写提示词 | 生成段与完成提示词代码块存在 |
| `coverage` | 填写画面对账 | **逐条对源核验**：行数与原文都要与剧本一致 |
| `validate` | 确定性校验 | `validate_delivery_md.py --script` 退出码为 0 |
| `review` | 全剧七遍语义二审 | 机器只保证上游仍有效，结论由人给 |
| `canvas` | 画布两阶段预检 | dry-run 与只读审计零硬错误（需用户授权）|
| `generate` | 顺序生成、验收、返工 | 需用户逐节点授权 |

交付 Markdown 一旦改动，`validate` 及其下游步骤**自动作废**（状态机比对内容哈希）——
避免"改完不重跑校验就交付"。改了内容就要重跑闸，没有例外。

反向也堵死了：直接跑 `validate_delivery_md.py` 而不走状态机时，校验器会检查同目录的
`<集号>-run_state.json` 流程凭据，缺失或前置步骤未完成一律报错。只想单独看某个文件时
显式加 `--standalone`，此时输出**不作为交付凭据**。

### 为什么要有这一层

规则写在文档里，执行者可以跳过而不被发现。EP1 的真实教训：道具提取、画面覆盖、色卡绑定
三条规则都白纸黑字写在本 skill 里，但一条都没落地，而且**没有任何机制发现没落地**——
台词有对账表所以零丢失，画面没有对账表所以丢了三成。差别不在难度，在有没有闸。

因此本 skill 的原则是：**能落成闸的规则，一律落成闸；只能靠自觉的规则，一律假定会被跳过。**

## 开始前：intake 前置决策问答

九步状态机的第一步是 `intake`，在读剧本、抽画面单元之前先做完。目的是把媒介、范围、
资产现状、画布操作意图、音色状态这几项决策一次问清楚——这些决策此前散落在流程各处
被动触发（跑到 1b 资产生产才发现没选 3D 子风格、跑到第 8 步才发现用户根本不想操作画布），
现在集中到开工前，用户不用被打断好几次。

**方法论：一次只问一题，等回答再问下一题**（照抄 `grilling` skill 的原话："Ask the
questions one at a time, waiting for feedback on each question before continuing. Asking
multiple questions at once is bewildering."）。区分**事实**和**决策**：

- **能从本地资料查到的是事实，别问，自己查**：这一集/这个项目是否已经跑过 intake（同项目
  其它集大概率沿用同一媒介/画布操作意图，别重新问一遍）；项目目录下是否已有色卡/人物图/
  音色等可复用资产；是否已有既有提示词或验收状态。本地已有资料先自行发现，不重复向用户索取。
- **只有用户能拍板的是决策，一个一个问**：
  1. 媒介（真人实拍／2D 动漫／3D CG／其它）——全剧统一声明后不中途更换，若同项目已有其它集
     声明过，确认是否沿用，不必重问；
  2. 若媒介＝3D CG：具体子风格（影视级国漫风格化3D／高端日韩风格化3D／仿真人数字人／其它），
     不接受"电影级、高精、PBR"这类共性词替代选择；
  3. 范围：只处理当前这一集，还是要走全剧连贯性检查；
  4. 资产现状：已有可复用资产，还是需要从零生产（决定是否要走 1b 的资产生产合同）；
  5. 画布操作意图：只生成 Markdown，还是需要同步到 TVMao 画布（决定后面是否要读画布合同）；
  6. 音色状态：说话人音色是否已全部就绪，还是需要走占位→关联两阶段。

问完把答案写成 `<集号>-任务前置决策.json`（schema 见下），再跑
`python3 scripts/pipeline_state.py check <集号> intake --dir <交付目录> --episode-no <集号数字>`。
机器闸只验证这五项（3D CG 时加第六项）是否都填了，不代替判断填得对不对——跟 `review`/
`canvas`/`generate` 那几步"机器只验证据、人判断语义"是同一分工原则。

```json
{
  "媒介": "真人实拍",
  "范围": "单集",
  "资产现状": "需要从零生产",
  "画布操作": "仅生成Markdown",
  "音色状态": "需要占位等待人工上传"
}
```

不要索取统一 `DURATION_SEC`。LLM 先按台词、动作、反应和剪辑落点判断每段时长，通常为 4–15 秒；若用户指定的模型有更窄时长集合，再以该模型实时 schema 为准（这条不属于 intake 决策，不在前置问答里问，正常走第 2 步切分生成段时判断）。

## 必读顺序

1. 完整读取 [v3 → LibTV 映射](references/v3-to-libtv-mapping.md)。
2. 完整读取 v3 的 [输入归一化](references/v3/01-input-normalization.md)、[实体提取](references/v3/02-entity-extraction.md)、[时长与切分](references/v3/03-duration-and-storyboard-splitting.md)、[景别运镜](references/v3/04a-shot-scale-and-camera-library.md)、[调度构图光线](references/v3/04b-blocking-composition-lighting.md)、[约束编译](references/v3/04c-constraint-and-negative-prompt-compiler.md)、[镜头编写](references/v3/04-shot-writing-and-directing.md)、[台词与声音](references/v3/05-dialogue-and-audio.md)和[标签风格](references/v3/06-tags-and-style-control.md)。
3. 完整读取 [制作就绪与返工合同](references/libtv/production-readiness-and-retake-contract.md)、[全剧提示词二审合同](references/libtv/series-continuity-audit-contract.md)、[LibTV 完成提示词规范](references/libtv/libtv-completed-prompt-format.md)、[Seedance 2.0 官方指南知识快照](references/libtv/seedance-2.0-official-guide-snapshot.md)、[Seedance 2.0 官方提示词优化适配](references/libtv/seedance-2.0-official-prompt-optimizer.md)、[真人表演、眼神与对白合同](references/libtv/performance-dialogue-contract.md)、[场景拓扑与功能朝向](references/libtv/scene-topology-and-functional-orientation.md)、[光影构图与画面风格母版](references/libtv/visual-look-and-style-bible.md)、[生成成片质量审计](references/libtv/generated-take-quality-audit.md)、[人工复制交付合同](references/libtv/manual-video-node-delivery.md)和 [Seedance 适配](references/libtv/seedance-2.0-adaptation.md)。
4. 完整读取 [万物生实战 Craft 补充](references/libtv/wanwu-field-craft.md)：过冲量、反英雄景别与音画距离耦合三项官方规范未覆盖、但被真实上线成片反复验证的手法；与既有合同冲突时以既有合同为准。
5. 项目没有现成素材、需要先造资产时，完整读取 [资产生产合同](references/libtv/asset-production-contract.md)。
6. 只有用户要求实际操作 TVMao CLI 时，才读取 [TVMao 画布合同（兼容文件名）](references/libtv/libtv-canvas-contract.md)并核对本机版本与模型 schema。

## 核心知识库模块

- 剧本与时长：内置 v3 归一化、原子事件和生成 Shot 预算；
- 表演与声音：眼神、身体层级、对白、音色和声画同出；
- 空间与连续性：场景母版、功能拓扑、家具/人体朝向、轴线和真实出点；
- 摄影与质感：`STYLE-ID`、`LOOK-ID`、光线、构图、材质和运动节奏；
- TVMao 执行：Mixed DSL 编译、稳定 modelId、有序入边、素材合规与运行前指纹凭证；
- 生成后验收：全部版本回收、十轴审计、全剧装配和单变量返工。

`scene-topology-and-functional-orientation.md` 是空间模块的强制组成部分，不能因已有场景图、
位置图或普通轴线账本而省略。缺失该文件时，tvskill 不具备完整迁移资格。

## 工作流

### 1. 建立内部账本

按原文顺序建立语音账本，覆盖所有有文本声音。建立导演弧线、场景母版、人群占位状态、原子镜头起落幅、人物动线、眼神对象、光线、道具实例与状态、相邻切点。对白节拍额外记录说话人的目标、障碍、策略、潜台词、开口触发、具体眼神目标、零或一个有动机的身体变化、零或一个必要声音控制和说完后的活状态。推理性账本不单独交付，**但其中可被校验的两项必须落进交付 Markdown**：资产清单（人物/场景/道具/色卡四类）与画面对账（原剧本每条画面指令的落点）。不落盘的约束等于没有约束——EP1 的画面丢失正是因为它只活在推理里。

全剧先锁一个 `STYLE-ID`，每个逻辑场锁一个 `LOOK-ID`，记录主光物理来源与世界方向、
对比和色彩、肤色与材质、镜头倾向、构图权力、运动节奏及同场变体。连续组重复同一条
短风格脊柱，不在各段随机更换“电影感”同义词。

同一逻辑场跨多段时规划场景包；锁定世界坐标、轴线、人物位置、主光方向、人群占位和关键道具实例。功能场景还必须锁定功能前方、家具朝向和人体默认使用方向：例如教室黑板/讲台在前墙，课桌、座椅和就座学生的骨盆、膝盖、双脚朝向黑板。相似道具必须编号，例如“钢笔-A（扎墙）”“钢笔-B（手中）”“报告-A-S0/S1/S2”，不得用一个模糊资产同时承担互斥状态。

### 1b. 提取实体与生产资产（无现成素材时）

用户已提供全部素材时跳过本步；否则按 [实体提取](references/v3/02-entity-extraction.md) 与 [资产生产合同](references/libtv/asset-production-contract.md) 补齐上游：

0. 先做风格选择闸：弹窗选择具体媒介子风格，只生成一张风格定调图；用户视觉确认后才建立正式资产 DAG。若选3D，必须明确是风格化动画、仿真人数字人或其它具体分支。风格定调图只决定 STYLE-ID，不得被误命名为人物标准参考图。

1. 从账本导出实体清单：可识别人物及其互斥形态、逻辑场及其人数与光线状态、承担叙事功能的道具及其互斥状态、每场色板、全剧媒介；
2. 按“镜头看到几个面就锚几个面”判定每个实体的资产形态；人物先产出与媒介匹配的标准设定资产，再派生视频可用的独立单人身份图。固定单侧可只派生单图，有转身、越肩、环绕或说话转头则补足对应角度；人物剧照不得反向替代标准设定资产，互斥形态与互斥状态各自独立成资产；
3. 为每个资产写结构化图生成提示词：格式、设定、世界统一 DNA、逐部位 CRITICAL、分格、光线、风格参考、失败模式定向负向；
   色卡是固定格式例外，必须直接套用资产生产合同中的万物生 13 色模板，不继承视频画幅；
4. 先低清试拍确认识别特征立得住，再高清定版；定版并经人工确认后写回账本作为 canonical；
5. 资产未验收前不得进入 Mixed 绑定，不得作为 canonical 被下游引用。

执行画布生成时，把所有资产节点按实际 ordered edge 做拓扑分层。只运行当前层：等待全部节点进入终态，回收并展示结果，逐张按七项 canonical 门槛验收；然后暂停并取得下一层的新授权。即使下一层的全部父图都已成功，授权弹窗出现前也不能提交。

提取结果必须写成交付 Markdown 的 `## 资产清单`，人物、场景、道具、色卡**四类都要有行**：
本集确实没有某一类时也要写一行"无＋已核对"，因为沉默无法与遗漏区分。两条硬要求：

- **承担叙事功能的道具必须登记**：被持有、被交接、状态变化、或作为关键信息载体的都算；
  纯陈设写明"由场景图承担"。需要精确可见文字的道具（报纸标题、刻字、通知单）备注写
  "含精确文字"，它必须有定版道具图并绑进对应段，该段文字策略必须为"定版道具图"——
  精确文字永远不交给视频模型现场拼字。
- **色卡登记了就必须绑**：色卡是视觉锚定，只在提示词里写 HEX 不成立。资产清单里有色卡
  而某段没绑定任何色卡资产，校验直接报错。

规划图（位置图、轨迹图、平面图、箭头图、故事板）只服务导演推理，永远不进 Mixed。

### 2. 切分生成段

每段只承担一个清楚的叙事任务。边界落在完成动作、完整意群、明确反应或自然转场。

- 先按官方任务分类判断多模态参考、编辑、延长或组合；只有 LibTV 实时 schema 暴露
  对应能力时才使用编辑/延长语义；
- 多模态参考再按时间与空间两个轴判断复杂度：两者都少才用单镜；多事件链、状态递进、
  对话往返、换机位或穿越多个区域任一成立时，优先在同一节点内使用多镜路径；
- 素材数量、节点秒数和是否已有镜号都只能辅助判断，不能因为 5–8 秒就强制判为单镜；
- 先按约 4–5 个汉字/秒核算台词；
- 再加入动作完成、反应、稳定起幅和完成落幅；
- 选择足够而不过度拖长的整数时长；
- 默认 4–15 秒，超出时在自然完成点拆段；
- 内部时间账本覆盖完整段长，但绝对时间码不得进入最终模型代码块。
- 同步对白镜头严格使用“每个 Shot 一位说话人一个自然意群”；只有单一事件时才要求单一连续镜头，多镜节点允许在不同 Shot 依次说话；
- 原生同步对白段不同时承担走位、转身、递物、高速道具、群体反应、精确文字、OS/VO 或复杂特效；
- 多镜声画同出节点中，每个 `Shot N:` 只允许一位说话人和一个自然意群；OS/VO 必须独立落在没有口型对白的 Shot；
- 每个 `Shot N:` 只选择一个主要保真目标：人物对白、复杂动作、群体状态或关键道具之一；其它层保持稳定；
- 一句超过一个自然换气意群或同时承担两次情绪转折时，在完整意群处拆段；
- 高速道具、精确文字、复杂接触和群演高风险变化仍分别拆段。

原剧本画面指令密集时，**唯一允许的做法是拆节点，不允许合并画面**。单个 `Shot N:` 最多
承载 2 条原剧本画面指令，第 3 条起校验报错；连续单镜可以一镜演完更多动作，但超过 4 条会
提示复核。"每个 Shot 只有一个主要保真目标"是**分配规则，不是删除许可**——被让出的那一层
必须有去处：拆到另一个 Shot、另一个节点，或在画面对账里显式写"舍弃＋理由"。直接让它消失
是本 skill 最严重的失误类型。

v3 的 2–3 秒内部剪辑节拍不等于模型 `Shot N:` 数量。原生 Seedance 多镜按生成事件预算：
10–15 秒通常只承载 2–3 个清楚 Shot，每个 Shot 约 4–6 秒完成一个动作、对白或反应。
内部剪辑点更多时，优先拆节点或交给后期，不把密集剪辑清单全部塞进一次生成。

同一时空的连续事件先尝试合并为一个多镜节点，避免人物在独立生成节点之间瞬移。单场
长对话、情绪递进或单一路径移动，若 LibTV 暴露视频延长能力，优先用上一段已验收视频
执行“向后延长”。只有剧情转折、新场景或高风险任务才采用受控独立节点。

### 3. 分配 Mixed 素材

先把资产分为“规划用”和“生成用”。规划图只帮助导演确定空间，绝不进入 Mixed。逐段按实际需要排列生成资产：

1. 主要人物独立身份图、按需使用的独立三分之四侧脸近照，以及该段开口人物各自独立音色；
2. 其他可识别人物各自独立的身份图，以及其中开口人物各自独立音色；
3. 干净首帧或上一段已验收的干净续接帧；
4. 与当前人数一致的场景状态图；
5. 当前状态的关键道具图或单一动作参考。

同一资产可在段内复用，但不同人物或不同资产类型不能共享 Mixed。全班可见时必须使用
经过视觉验收的“占座＋功能朝向”场景状态图，能看清黑板/讲台、桌椅统一朝向、学生坐姿、
门窗和主过道；空教室图不得与可见群演要求同段，也不能单独承担群像坐姿方向。
所有 `{{Mixed N}}` 从 1 连续递增；语义名保持稳定。

对白人物只有正面立正、直视镜头的全身图时，不能直接进入正式近景对白。保留该图承担身份与服装，同时增加离镜视线的三分之四侧脸图或干净过肩首帧；否则降级为预览。任一文本声音没有合法音色素材时，节点保持阻塞，不得以关闭声音绕过。

#### 音色占位与关联

音色由用户本人决定并上传，本 skill 不替用户挑定，也不因此停下上游工作。两个阶段：

1. **占位**：说话人音色未上传时，在 Mixed 表写一行占位槽——素材"<角色名>-音色占位槽（待人工上传）"、
   类型"音频（待上传）"、绑定语义写该角色的音频语义名；段头写 `- 音色状态：待关联｜占位槽：…`，
   `- 运行状态：阻塞`。提示词照常写"只控制 `<主体N>` 的音色，不继承原台词、背景声、情绪和节奏"。
   占位槽只存在于尚未同步的 Markdown 中，不创建空节点，不编造资产 ID。
2. **关联**：用户把音色素材上传到画布后，按实时事实回填，不靠计划表：

```bash
tvmao node list --project <PROJECT_ID>     # 找到用户上传的 audio-input 真实节点 ID
python3 scripts/validate_delivery_md.py <集号>-LibTV视频节点提示词.md
```

   先用 `node list` 回读真实 audio-input ID，把占位行换成真实素材名与类型"音频"，音色状态改为
   "已绑定<素材名>"，运行状态按前置依赖改回可运行或等待上段验收；再跑校验。同步时用
   `--asset '素材名=<节点ID>'` 显式关联，并按 `edge list --to` 的实时顺序验证；不得靠节点名猜测。

★**内联音频素材不一定是画布节点**：以上流程假设音色素材是可被 `node list`/`edge list`
回读到的独立画布节点；但实测存在音频素材直接以内联形式上传到某个节点（有 label 标识、
但本身不是一个独立节点，`node list` 查不到）的情况。遇到 `node list` 回读不到用户已上传
的音色素材、但同步/生成侧又能看到它生效时，先怀疑是内联素材而不是"用户没传成功"；
向用户确认素材的真实挂载方式，不要在没查清楚前就把"待关联"状态强行标成失败或反复要求
用户重传。这条路径目前没有确定性回读命令，只能人工核对。

用户尚未提供音色时，可从同项目已验收成片抽取候选样本供其试听，但选定权始终在用户；
未经用户确认的候选音色不得写成"已绑定"。

可从同一项目已验收的历史 Seedance 2.0 成片建立音色资产：为每名角色选取只有该角色
说话、无其他人声、无音乐且背景声尽量低的连续有效人声，裁成约 5 秒，导出 48 kHz 单声道
WAV，并以“角色名-历史成片音色参考-5s”独立上传。不得把多人对话整段、含串音片段或
同一音频绑定给多个角色。提示词必须声明该音频只控制指定 `<主体N>` 的音色，不继承
原台词、背景声、情绪和节奏；原台词仍以当前 `{}` 内容为唯一真值。

选好单人说话时间窗后，用内置脚本确定性裁切并校验：

```bash
python3 scripts/extract_voice_reference.py <历史成片.mp4> <角色名-历史成片音色参考-5s.wav> \
  --start <有效人声起点秒数> --duration 5
```

脚本依赖本机 `ffmpeg`，只负责格式、时长和响度处理，不能自动判断是否串入其他人声；
上传前仍须人工试听确认单一说话人、无音乐和无明显背景干扰。

### 4. 编写 LibTV 完成提示词

最终提示词使用紧凑三段论：

1. **总体设定与资产定义**：按官方主体定义法绑定独立人物图；TVSkill 默认统一为 `<主体N>`，角色真名保留在 `@[角色名] {{Mixed N}}` 中。把当前人数场景状态定义为 `<场景N>`，用干净首帧/续接帧锁定站位、眼神和道具状态。★本质容易被模型误判的实体（机器人、非人生物、义体、非常规物种等——只描述外观容易被当成"戴了装置的人类"或"变异人类"而不是原设定的本质）定义时补一句**排除式定义**："是 X 而非 Y"，把本质钉死在描述之外（如"是人形机器人而非人类，机械头部…"），比单纯堆外观特征更稳；本质清楚的普通人类角色不需要这条，不为所有角色套用。
2. **镜头动作**：连续单镜写明“单一连续镜头，无剪切”，不使用镜号；原生音频同步对白包含开口触发、具体眼神对象、精确台词和活状态。身体变化与声音控制都允许为零且各最多一个。短台词使用一口自然说完，不写放慢、停半拍、一字一顿或拖长。时间或空间维度为“多”时使用精确 `Shot 1:` / `Shot 2:` 标签，可在冒号后补中文分镜名；每个含文本声音的 Shot 只出现一名说话人并使用其独立音色，每个 Shot 只有一个主要保真目标；10–15 秒通常为 2–3 个 Shot。禁止旧 `镜头N：` 标签，也不能把连续镜头合同与 `Shot N:` 混用。
3. **风格与约束**：沿用本场 `LOOK-ID` 的短风格脊柱，写明物理光源、构图关系、真实材质和运动节奏，再保留连续性、零文字、水印/Logo 和必要的多人身份约束；分辨率交给节点参数，不堆叠空泛画质词或普通缺陷词。

多人镜头采用三级动作：一个焦点人物完成主要动作，最多一人完成一次小反应，其余人物只保持呼吸、眨眼、轻微肩部变化或自然视线漂移。不得写全场同时转头、同时僵住或同时后退。

功能场景的群像反应还要锁住身体层级。教室里“学生看向某人”默认只移动眼睛、头部和
最多少量肩部；骨盆、膝盖、双脚、座椅和课桌仍朝黑板。后排朝前墙机位可见黑板时，
正常就座学生只能呈背面或后侧三分之四；学生正脸完整可见时，黑板应在摄影机后方或改用
侧面机位。不能为了正脸让学生整排反坐。

正式段在三段论之上必须携带结构六件套：开篇风格锁定行、逐资产视觉锚定语（"严格按此图渲染，不可改造"）、
`【阶段N…】` 段内节拍与 inline `#RRGGBB`、独立 `【声音设计】` 分层段、`【关键约束】` 具名铁律、
结尾 `NOT` 链。六件套依附三段论、不替换官方主体定义法与台词真值规则，逐条写法见
[LibTV 完成提示词规范](references/libtv/libtv-completed-prompt-format.md) 的"正式段结构六件套"。

台词使用 `{}`，音效使用 `<>`，BGM 使用 `（）`；代码块中的 `【】` 只允许 `【阶段N…】`、`【声音设计】`、`【关键约束】` 三类结构标记，其余 `【…】` 一律禁止。绝对时间码仍然禁止进入代码块，唯一例外是 `【声音设计】` 段内部的段内相对秒层。完整遵循 [官方提示词优化适配](references/libtv/seedance-2.0-official-prompt-optimizer.md)。

### 5. 生成 Markdown

复制 [交付模板](assets/libtv-video-prompts.template.md)并严格遵循 [人工复制交付合同](references/libtv/manual-video-node-delivery.md)：

- 全集摘要、段数和总时长；
- 节点默认模型 `Seedance 2.0 Fast VIP`、画幅 `9:16` 与分辨率 `480P`；
- 公共素材清单；
- 按顺序排列的全部生成段；
- 每段 Mixed 上传顺序；
- 每段交付等级、制作路线、风险标签、运行状态、连续性模式、声音开关和文字策略；
- 每段逐说话人音色绑定状态；音色待人工上传时写明占位槽与"待关联"，并把该段标为阻塞；
- 每段一个可整块复制的最终提示词代码块；
- 相邻段衔接检查；
- 原始语音逐句对账；
- 资产清单（人物/场景/道具/色卡四类齐全）；
- 画面对账（原剧本每条画面指令一行，落点写到 V##-Shot#，或转后期叠字、或舍弃＋理由）。

交付 Markdown 的**必需章节**（缺一即校验失败，以校验器为准）：
`## 使用方法`、`## 公共素材清单`、`## 资产清单`、`## 全剧连续性声明`、`## 全剧连续性母版`、
`## 段间衔接总表`、`## 语音对账`、`## 画面对账`。
每个生成段内必需：`### Mixed 上传顺序`、`### LibTV 完成提示词（整块复制）`、`### 衔接`、
`### 状态交接`（至少一行有效状态）、`### 剧本事实对账`（至少一行，且结果全为"通过"）。
全剧连续性声明必需字段还包括 `剧情时间锚`、`前集承接`、`本集最终出点`。
以上以 `assets/libtv-video-prompts.template.md` 为唯一可复制骨架——照模板抄，不要自行拼装。

### 6. 全剧二次导演检查

提示词初稿完成后不得直接交付。读取同项目全部可用剧本、既有集 Markdown 和已验收
观察状态，严格执行 [全剧提示词二审合同](references/libtv/series-continuity-audit-contract.md)
的七遍审核：原文准确性、原子任务、资产职责、段内一致性、段间连续性、跨集连续性和
模型可执行性。

先生成问题清单，再修改提示词、Mixed、制作路线和状态交接；修改后从第一遍重新检查。
全剧资料齐全时统一审核全部集；只提供单集时只能写“截至当前集”或“单集”，不得假定
未来剧情。每集 Markdown 保留全剧连续性声明、连续性母版、每段连续组/前置段和状态
交接表，但不另交付 JSON。

### 7. 确定性校验

```bash
python3 scripts/validate_delivery_md.py <集号>-LibTV视频节点提示词.md \
  --script <原剧本.docx> --episode <集号数字>
python3 scripts/audit_series_continuity.py <剧集提示词目录或多个Markdown>
python3 scripts/validate_skill_portability.py .
```

修正全部错误，直到退出码为 0。只有确定性校验和 LLM 七遍语义二审都通过，才能把
`全剧二审` 与各段 `提示词二审` 写为 `已通过`。脚本通过不代表模型成片合格。

### 8. TVMao 画布两阶段预检

只有用户明确要求发送到 TVMao 画布时执行本节，且创建/更新节点与运行生成必须分成两个阶段：

1. 创建或更新**未运行**的视频节点，连接各段实际生成资产；不得包含 `--run`；
2. 重新读取每个节点的稳定 `modelId`、扁平 params、状态、实时有序入边与素材合规状态；
3. 不信任上传表自报的顺序；必须按 `edge list --to` 回读。Markdown Mixed 写入时编译成携带真实节点 ID 的 canonical mention，模型侧编号按实时入边确定性序列化；
4. 对目标节点执行只读预检：

```bash
python3 scripts/sync_delivery_markdown.py <本集.md> \
  --project <PROJECT_ID> \
  --asset '素材名=n-节点ID'
# 核对 dry-run 后，用户已明确要求写画布时再追加 --apply；更新旧节点还需 --node V01=n-节点ID

python3 scripts/audit_canvas_nodes.py --project <PROJECT_ID> --node <NODE_ID> \
  --asset '素材名=n-节点ID'
```

必须用一个或多个 `--node <NODE_ID>` 精确限定节点；只有明确要求全项目时才用 `--all`。
任何硬错误都必须修复、回读并重审；只有 `errors=0` 才能报告“画布提示词已就绪”。
Seedance 素材还必须经 `tvmao compliance verify/status` 变为 `active`。准备运行时追加 `--pre-run` 和
`--receipt <审计凭证.md>`；凭证记录当前节点指纹，节点 prompt、modelId、扁平 params、
有序入边、合规状态或任务状态发生变化后立即失效。已有生成结果的节点若未通过审计，其成片和尾帧一律视为
拒绝态，不能进入连续性链。

只有用户另行明确授权运行具体节点后，才按同一连续组顺序生成。不得并行修改同一画布；
写入后必须以回读事实为准，不能用计划表或 CLI 成功返回替代验收。

### 9. 顺序生成、成片回收、验收与返工

资产图先执行“层内可并行、层间强暂停”：同层节点彼此无依赖时可并行；任一下一层节点必须等全部所需父图成功、视觉验收通过、`audit_image_asset_node.py --pre-run` 零错误，并收到用户对该层的新授权。上一层的生成授权不向下继承。

同一连续组只运行当前“可运行”的第一段。生成后逐段检查身份、眼神目标、口型与逐字台词、动作边界、人群数量、道具实例/状态、空间轴线、画面文字和相邻可剪辑性。任何硬失败必须标为拒绝，不能作为下一段参考。

每轮生成完成后，按 [生成成片质量审计](references/libtv/generated-take-quality-audit.md)
下载目标前缀下所有已生成版本，不只看最新节点。保留节点原名，生成接触表并读取音轨；
逐片执行节点参考、人物、表演眼神、对白声音、空间人群、道具物理、镜头剪辑、光影质感、
画面卫生和边界连续性十轴审计。离线 ASR 只定位复听疑点，不代替人工逐字验收。

单片通过后还要按剧本顺序装配，检查同场人口、空间、光线、色彩、音色、动作因果和
相邻首尾帧。只有“保留”片段可输出稳定续接帧；预览、后期候选、重抽和重写片段都不能
自动进入连续性链。

返工必须改动失败变量：更换干净首帧、拆段、调整制作路线、补充或更换角色音色、改用后期文字或替换冲突资产。相同硬失败再次出现时，不允许只追加“不要……”类否定词重跑。验收通过后才导出干净稳定续接帧，并与 canonical 人物、场景和道具母版共同使用；尾帧只允许一跳，每两到三段重新锚定 canonical。

★**后期补救节点（不必重跑生成）**：万物生·问心画布出现两类正传没有的后期处理节点——
`remove-subtitle`（去字幕，用于清掉模型自带、prompt 里明确禁止过但仍然生成出来的字幕/水印
残留）与 `expand`（扩图/outpainting，画幅或构图边界不够时向外补齐，不重新生成主体内容）。
这两类**只解决"画面本身没问题、只是边角有瑕疵"的情况**，不能用来掩盖硬失败（人物崩坏、
台词错误、道具状态矛盾这类仍必须走上面的"改动失败变量"重新生成，不可能靠后期节点修复）。
使用前必须先 `tvmao model get/list` 回读这两类节点的实时 `available` 与 `inputSchema`——
本 skill 目前没有为它们建立像 `video-generator`/`image-generator` 那样的确定性校验器，
用之前要先确认真实参数结构，不可比照本文件其它节点的参数名称硬猜。

## 交付标准

- 交付目录只新增一个 Markdown 成品；不产出 `candidate.json`、`asset-plan.json`、`movement-ledger.json`、`reference-manifest.json` 这类中间态 JSON。流程状态机的 `<集号>-run_state.json` 与 `<集号>-画面单元.json` 是**流程凭据不是交付物**，必须与交付 Markdown 同目录（校验器要靠它判断流程有没有走完），交付给下游时不随附；
- Markdown 包含真实审核范围、全剧连续性母版、每段前置依赖和状态交接；
- 全剧资料齐全时跨全部集二审；资料不全时明确写“截至EPXX”或“单集”；
- 确定性校验与七遍语义二审全部通过后才允许标记“已通过”；
- 生成段编号连续，总时长等于各段时长之和；
- Markdown 明确标注默认模型 `Seedance 2.0 Fast VIP`、画幅 `9:16`、分辨率 `480P`；
- 每段 Mixed 上传表与代码块中的 `{{Mixed N}}` 一一对应且连续；
- 发送画布后，Markdown Mixed 必须已编译为 TVMao node-id mention，并与实时有序入边的类型、编号和素材 ID 一致；任何普通 `@图片N` 文本都不算画布关联，画布只读审计必须零硬错误；
- 运行前审计凭证必须与当前节点指纹一致；任一字段变化后重新审计，不沿用旧凭证；
- 段时长只存在于 Markdown 与节点参数，代码块不出现绝对时间码；
- 声音默认开启；每个含文本声音的 `Shot N:` 只有一位说话人和一个自然意群，且绑定该人物独立音色；
- 同步对白包含开口触发、具体眼神对象、精确台词和活状态；零或一个身体变化、零或一个声音控制；
- 多人镜头只设一个表演焦点，背景人物使用持续微动，不出现同步群体反应；
- 功能场景的前方、家具朝向与人体默认朝向一致；教室群像绑定已验收的占座教学朝向图，
  学生回看只分级转动眼、头、肩，不改变骨盆、膝盖和课桌朝向；
- 每个可识别人物使用独立人物图，规划图不进入 Mixed，干净生成资产各司其职；
- 台词、OS、VO、旁白逐说话人绑定合法独立音色；音色未上传时写明占位槽并标阻塞，关联真实音频后才可运行，不得关闭原声绕过，也不得由助手代选音色；
- 正式段提示词携带结构六件套：风格锁定行、逐资产视觉锚定语、阶段节拍与 inline HEX、独立声音设计段、具名铁律、结尾 NOT 链（**机器只给警告不拦截**——结构在不在机器能判，写得对不对判不了；这一条靠人工与成片验收把关，升级成硬错误前须先全季回归）；
- 台词逐字保留且只出现一次；
- 画面对账逐条对源核验通过：原剧本每条画面指令都有落点或显式处置，无一行缺失或被改写；
- 资产清单四类齐全，含精确文字的道具有定版道具图，已登记的色卡在每段都完成绑定；
- 每张色卡参考图均为万物生固定版式：16:9 横版白底、13 色单排、等大纯色色块、黑色等宽 HEX 标签；视频画幅不得写入色卡格式；生成后已打开实图逐格核对色块数量、排列、标签内容及顺序，不能只凭任务成功状态验收；
- 流程状态机九步全部 done，且交付 Markdown 的内容哈希与校验时一致；
- 相邻段的人物、轴线、道具、光线、环境声和动作状态可剪辑衔接；
- 同一场景连续组沿用同一 `LOOK-ID`，主光方向、色彩、材质和运动节奏无随机漂移；
- 全班/人群可见时使用已占位场景状态；精确文字只用定版道具图或后期叠加；
- 硬失败片段不交付、不续接；续接帧来自已验收片段且只允许一跳；
- 每轮生成后回收所有已生成版本并完成十轴逐片审计与按剧本顺序的装配复核；
- 所有段显式声明全剧统一的媒介（真人实拍／2D 动漫／3D CG／其它），媒介与资产图一致且中途不更换；物理光源、零字幕、零水印、零 Logo，不用空泛画质套话替代表演；
- 未获明确授权时，不写画布、不运行生成。
